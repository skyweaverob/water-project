import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "500", "600"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Aquaprice — Procurement intelligence for water treatment chemicals",
  description:
    "Buy-side procurement intelligence for municipal utilities and industrial pretreatment operators. Write better RFPs, evaluate bids, monitor supply chain risk.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
